# Sentiment-Analyzer
I just created a project called "Sentiment Analyzer" that analyzes the sentiment expressed in videos. It extracts speech from uploaded videos and converts it into timestamped text to classify the sentiment as Positive, Neutral, or Negative with confidence scores.  
